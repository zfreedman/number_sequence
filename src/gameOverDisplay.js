import React from "react"

class GameOverDisplay extends React.Component {
  render() {
    return (
      <button className="gameOverDisplay">
        Game Over
      </button>
    );
  }
}

export default GameOverDisplay;
